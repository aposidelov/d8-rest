<?php

/**
 * Implements hook_theme().
 */
function bt_rest_json_theme() {
    return [
        'bt_rest_json_simple_form' => [
            'render element' => 'form',
            'template' => 'simple-form',
        ],
    ];
}

/**
 * Implements hook_preprocess_bt_rest_json_view().
 */
/*function bt_rest_json_preprocess_bt_rest_json_view(&$variables) {
    //kint($variables);
    $item = &$variables['elements']['bt_rest_json']['#value'];
    $variables['title'] = $item->title;
    $variables['price'] = $item->price;
    $variables['sku'] = $item->sku;
    $variables['ajax_link'] = $variables['elements']['ajax_link'];
    $variables['extra_values'] = array( 'key1' => t('Item 1'),
                                        'key2' => t('Item 2'),
                                        'key3' => t('Item 3'));
}*/